
function Gallery () {
    return (
        <div>
            There will be a component with a flex box with links to properties. each property will have its own page to be developped.

        </div>
    );
}
export default Gallery;